import { CheckCircle2, XCircle } from 'lucide-react';
import { Link, useLocation } from 'react-router-dom';
export function BillingResultPage() { const success = useLocation().pathname.endsWith('/success'); return <div className="invite-page"><section className="invite-card billing-result"><img src="/fitlab-logo.png" alt="FitLab"/>{success ? <CheckCircle2 className="result-success"/> : <XCircle className="result-cancel"/>}<h1>{success ? 'Pago recibido' : 'Proceso cancelado'}</h1><p className="muted">{success ? 'Stripe está confirmando la suscripción mediante el webhook. El estado se actualizará en unos segundos.' : 'No se realizó ningún cambio. Puedes intentarlo nuevamente cuando quieras.'}</p><Link className="primary" to="/billing">Volver a la suscripción</Link></section></div>; }
