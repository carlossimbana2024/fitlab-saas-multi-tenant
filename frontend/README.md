# FitLab Frontend

React + TypeScript + Vite. Usa exclusivamente `VITE_API_URL`; las claves privadas permanecen en el backend.

```powershell
npm install
Copy-Item .env.example .env
npm run typecheck
npm run build
npm run dev
```

El logo debe guardarse como `public/fitlab-logo.png`.
